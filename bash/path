. $DOTFILES/common

# ~/.local/ paths
prepend PATH $HOME/.cargo/bin
prepend PATH $HOME/.local/override-bin
append PATH $HOME/.local/bin
append PATH $HOME/.local/opt/fast-export

# add .local/lib to LD_LIBRARY_PATH
prepend LD_LIBRARY_PATH $HOME/.local/lib

[[ -f ~/.local/bash_path ]] && . ~/.local/bash_path
