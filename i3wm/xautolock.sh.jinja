#!/bin/sh
#
# Managed by {{ dotfiles_managed }}
# Variables from {{ dotfiles_source }}
#

exec xautolock \
  -detectsleep \
  -time "{{ autolock_timeout }}" \
  -locker "{{ autolock_locker }}" \
  -killtime "{{ autolock_killer_timeout | default('30') }}" \
  -killer "{{ autolock_killer | default('/bin/true') }}"
